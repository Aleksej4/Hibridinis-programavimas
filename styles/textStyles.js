import { StyleSheet } from "react-native";

export const textStyles = StyleSheet.create({
    buttonText: {
        color: '#000000',
        fontWeight: 'bold',
        fontSize: 16
    },

    titleText: {
        color: '#000000',
        fontWeight: 'bold',
        fontSize: 25
    }
})