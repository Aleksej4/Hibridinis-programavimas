import { StyleSheet } from "react-native";

export const screenContainersStyles = StyleSheet.create({
    mainScreen: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    }
})